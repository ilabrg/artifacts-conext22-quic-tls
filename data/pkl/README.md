This folder will contain compressed dataframes for fast I/O used during analysis.

We use zstd-compressed, binary pickle files: `dataframe.pkl.zst`

Hence, these files are most-likely system-specific.
